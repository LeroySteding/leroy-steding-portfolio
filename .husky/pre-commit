# Run lint-staged in portfolio app (only if portfolio files are staged)
if git diff --cached --name-only | grep -q "^apps/portfolio/"; then
  cd apps/portfolio && pnpm lint-staged
  cd ../..
fi

# Skip typecheck for now (too many existing errors)
# cd ../.. && pnpm typecheck
